# backupomat

## Node.js Backup retrieval utility using the Compose API

backupomat.js is the simple version for backup archive retrieval

backupomatplus.js is the version which starts an on demand backup, waits for it to complete, then retrieves it.
